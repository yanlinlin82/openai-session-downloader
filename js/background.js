/*
chrome.runtime.onMessage.addListener(function(request, sender, sendResponse) {
    if (request.type === 'DOWNLOAD_SESSION') {
        let data = request.data;
        // 处理Markdown转换，打包ZIP和下载
        // 你可能需要额外的库和函数来实现这些功能
    }
})
*/